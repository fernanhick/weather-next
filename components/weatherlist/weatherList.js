import React from 'react'

function WeatherList({ data }) {
    return (
        <>
            {/*    {data &&
                data.map((loc, index) => {
                    return (
                        <div key={index}>
                            <p>{loc.city}</p>
                        </div>
                    )
                })} */}
        </>
    )
}

export default WeatherList
